using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestroyController : MonoBehaviour
{
    public GameObject canvas;

    // Update is called once per frame
    //void Update()
    //{
    //    if (Health.destroyControl)
    //    {
    //        canvas.SetActive(true);
    //        canvas.transform.Find("Panel").gameObject.SetActive(false);
    //        canvas.transform.Find("DestroyPanel").gameObject.SetActive(true);
    //        Health.destroyControl = false;
    //    }
    //    if (Health.respawnControl)
    //    {
    //        canvas.SetActive(false);
    //        Health.respawnControl = false;
    //    }
    //}
}
